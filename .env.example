# MOMO Air Care — environment variables
# Copy to .env.local for local development. Never commit real secrets.

# Where quote-form leads are emailed (FormSubmit destination).
# The address must be activated with FormSubmit before delivery works.
LEAD_NOTIFY_EMAIL=leads@example.com
